# A9Bot Embedded Runtime

This directory contains the A9Bot runtime embedded by the A9 Marketing System.

The outer A9MS server starts it from `server.js` and serves the built WebUI from
`a9bot/web/dist`.
